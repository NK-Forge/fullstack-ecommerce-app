function LoginPage() {
  return (
    <main>
      <section className="panel">
        <p className="eyebrow">Account</p>
        <h1>Login</h1>
        <p>
          The login form will connect to the existing authentication endpoint in the next auth step.
        </p>
      </section>
    </main>
  );
}

export default LoginPage;